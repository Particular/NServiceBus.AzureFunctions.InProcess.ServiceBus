assembly-versioning-scheme: Major
branches:
  master:
    mode: ContinuousDeployment
    tag: experimental
    increment: Minor
    tracks-release-branches: true
  release:
    increment: Patch
    tag: ''
